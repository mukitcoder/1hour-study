import React from 'react';
import Courses from '../Courses/Courses';
import Hero from '../Hero/Hero';

const Home = () => {
    return (
        <div>
            <Hero/>
            <Courses/>
        </div>
    );
};

export default Home;